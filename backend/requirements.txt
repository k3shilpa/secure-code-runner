# === Core Web Framework ===
fastapi==0.116.1         # FastAPI framework for building backend APIs
uvicorn==0.35.0          # ASGI server to run FastAPI apps
starlette==0.47.3        # Core toolkit FastAPI is built on (routing, middleware)

# === Pydantic (Data Validation & Schemas) ===
pydantic==2.11.7         # Data validation / parsing for request & response models
pydantic_core==2.33.2    # Internal core for pydantic performance
annotated-types==0.7.0   # Support for Python type hints like Annotated
typing-inspection==0.4.1 # Helpers for working with typing/annotations
typing_extensions==4.15.0 # Backports of newer Python typing features

# === Networking & Concurrency ===
anyio==4.10.0            # Async networking backend (used by FastAPI/Starlette)
h11==0.16.0              # Pure-Python HTTP/1.1 library (used by uvicorn)
sniffio==1.3.1           # Detect async library in use (trio/asyncio/curio)
idna==3.10               # Internationalized domain name support

# === CLI & Dev Tools ===
click==8.2.1             # For building CLI commands (uvicorn uses this)
colorama==0.4.6          # Cross-platform colored terminal output

# === AI / Machine Learning ===
transformers==4.56.1     # Hugging Face Transformers library (loads CodeT5 model)
torch==2.8.0             # PyTorch backend required for model execution
accelerate==0.34.2       # Optimized inference utilities for Hugging Face models

# === Utilities ===
requests==2.32.3         # HTTP requests (used internally by Hugging Face + general purpose)
